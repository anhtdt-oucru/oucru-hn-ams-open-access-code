options(renv.config.sandbox.enabled = FALSE) #debug for sandbox dll error
options(renv.config.cache.enabled = FALSE)  # disable cache lookup
options(shiny.maxRequestSize = 100 * 1024^2)  # max file size 100mb, changeable
source("renv/activate.R")
